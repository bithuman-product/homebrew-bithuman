name: bithuman_engine_protocol
description: "Shared Dart contract (EngineDescriptor + registry) for the bitHuman unified multi-engine architecture (Layer 0)."
version: 0.1.0
homepage: https://www.bithuman.ai
repository: https://github.com/bithuman-product/bithuman-engine-protocol

environment:
  sdk: ^3.11.5

# Zero runtime dependencies — this is a pure-Dart descriptor/registry package.
dependencies:

dev_dependencies:
  flutter_lints: ^6.0.0
