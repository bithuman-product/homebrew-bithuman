[project]
name = "bithuman-mcp"
version = "0.3.4"
description = "DEPRECATED — use `bithuman mcp` in the bitHuman CLI. MCP server for the bitHuman AI avatar platform."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [{ name = "bitHuman", email = "hello@bithuman.ai" }]
keywords = ["bithuman", "mcp", "model-context-protocol", "avatar", "ai", "tts", "agent"]
classifiers = [
    "Development Status :: 7 - Inactive",
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
]
dependencies = [
    # <2: mcp 2.0 removed mcp.server.fastmcp — an unbounded pin made fresh
    # installs crash at import (ModuleNotFoundError). Cap until/unless this
    # (deprecated) server is ported to the 2.x API.
    "mcp[cli]>=1.2.0,<2",
    "httpx>=0.27",
]

[project.urls]
Homepage = "https://bithuman.ai"
Documentation = "https://docs.bithuman.ai"
Source = "https://github.com/bithuman-product/homebrew-bithuman/tree/main/packages/python-mcp"

[project.scripts]
bithuman-mcp = "bithuman_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bithuman_mcp"]
